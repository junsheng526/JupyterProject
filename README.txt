1. Before run the program, need to install some libraries:

pip install matplotlib
pip install numpy
pip install pandas
pip install seaborn
pip install tensorflow
pip install os

conda install -c conda-forge keras

2. Run the 'EmotionClassification.ipynb' to train the model. 

3. Run the 'main.ipynb' to open camera and detect your emotions. 
